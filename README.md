# Credit Risk Prediction — Home Credit (Application Data)

This project builds an interpretable credit risk model to predict loan default risk using the Home Credit application dataset (`application_train.csv`).

## Status
In progress.
